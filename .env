# =========================
# Plugin Settings
# =========================

# The human-readable name of your plugin as shown in the WordPress Plugins page.
# Required. Fallback order at runtime:
# 1. VITE_PLUGIN_NAME (if set and non-empty)
# 2. "My Plugin" (hardcoded default)
VITE_PLUGIN_NAME=

# The current version of your plugin. Used in the plugin header and for cache-busting assets.
# Optional. Fallback order at runtime:
# 1. VITE_PLUGIN_VERSION
# 2. "1.0.0"
VITE_PLUGIN_VERSION=

# A short description of what your plugin does. Appears in the Plugins page.
# Optional. Fallback order at runtime:
# 1. VITE_PLUGIN_DESCRIPTION
# 2. "No description provided."
VITE_PLUGIN_DESCRIPTION=

# The URL for your plugin’s homepage or documentation.
# Optional. Fallback order at runtime:
# 1. VITE_PLUGIN_URI
# 2. ""
VITE_PLUGIN_URI=

# The name of the plugin author. Appears in the Plugins page.
# Optional. Fallback order at runtime:
# 1. VITE_PLUGIN_AUTHOR
# 2. ""
VITE_PLUGIN_AUTHOR=

# The URL for the plugin author’s homepage or profile.
# Optional. Fallback order at runtime:
# 1. VITE_PLUGIN_AUTHOR_URI
# 2. ""
VITE_PLUGIN_AUTHOR_URI=

# Text domain used for localization and translations.
# Optional. Fallback order at runtime:
# 1. VITE_PLUGIN_TEXT_DOMAIN
# 2. "my-plugin"
VITE_PLUGIN_TEXT_DOMAIN=

# The path to your translation files relative to the plugin root.
# Optional. Fallback order at runtime:
# 1. VITE_PLUGIN_DOMAIN_PATH
# 2. "/languages"
VITE_PLUGIN_DOMAIN_PATH=


# =========================
# Menu / Page Settings
# =========================

# The label that will appear in WordPress admin menus or in templates.
# Optional. Fallback order at runtime:
# 1. VITE_PLUGIN_MENU_NAME (if set and non-empty)
# 2. VITE_PLUGIN_NAME (if set and non-empty)
# 3. "My Plugin" (hardcoded default)
VITE_PLUGIN_MENU_NAME=

# If set, your plugin will appear as a submenu under the specified parent menu slug.
# Leave empty to register as a top-level menu. Fallback order at runtime:
# 1. VITE_PLUGIN_PARENT_MENU_SLUG
# 2. null (top-level menu)
VITE_PLUGIN_PARENT_MENU_SLUG=

# The required WordPress capability a user must have to access this menu.
# Example: 'manage_options' (admins), 'edit_posts' (editors), etc.
# Optional. Fallback order at runtime:
# 1. VITE_PLUGIN_MENU_CAPABILITY
# 2. "manage_options"
VITE_PLUGIN_MENU_CAPABILITY=

# Position of the top-level menu in the admin sidebar.
# Lower numbers appear higher in the menu. Ignored if this is a submenu.
# Optional. Fallback order at runtime:
# 1. VITE_PLUGIN_MENU_POSITION
# 2. 25
VITE_PLUGIN_MENU_POSITION=


# =========================
# Development / Build Settings
# =========================

# The port used by Vite’s dev server during development.
# Optional. Fallback order at runtime:
# 1. VITE_PLUGIN_PORT
# 2. 3000
VITE_PLUGIN_PORT=

# Whether to include optional assets (like Vue.js) in your plugin build.
# Optional. Fallback order at runtime:
# 1. VITE_PLUGIN_INCLUDE_OPTIONAL_ASSETS
# 2. false (not included by default)
VITE_PLUGIN_INCLUDE_OPTIONAL_ASSETS=
